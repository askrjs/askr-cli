{
  "name": "{{appName}}",
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vp dev",
    "build": "vp build",
    "preview": "vp preview",
    "test": "npx vite-plus test run",
    "test:watch": "vp test --watch",
    "type-check": "tsc --noEmit",
    "lint": "npx vite-plus lint .",
    "lint:fix": "npx vite-plus lint --fix .",
    "fmt": "npx vite-plus fmt .",
    "fmt:check": "npx vite-plus fmt --check .",
    "check": "npx vite-plus check && npx vite-plus test run"
  },
  "dependencies": {
    "@askrjs/askr": "0.1.0",
    "@askrjs/ui": "0.1.0",
    "@askrjs/themes": "0.1.0",
    "@askrjs/lucide": "0.1.0"
  },
  "devDependencies": {
    "@askrjs/vite": "0.0.1",
    "@types/node": "^25.5.0",
    "jiti": "^2.6.1",
    "jsdom": "^29.0.1",
    "typescript": "^6.0.2",
    "vite-plus": "^0.1.19"
  }
}
